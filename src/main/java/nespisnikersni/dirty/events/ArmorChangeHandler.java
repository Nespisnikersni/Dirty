package nespisnikersni.dirty.mixin.events;
import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.entity.player.PlayerInventory;
import net.minecraft.item.ItemStack;

public class ArmorChangeHandler {

//    public static void register() {
//        PlayerE.EVENT.register((player, slot, previousStack, currentStack) -> {
//            if (isArmorSlot(slot) && previousStack != ItemStack.EMPTY && currentStack == ItemStack.EMPTY) {
//                ArmorRemovedCallback.EVENT.invoker().onArmorRemoved(player, previousStack);
//            }
//        });
//    }
//
//    private static boolean isArmorSlot(int slot) {
//        return slot >= 5 && slot <= 8;
//    }
}

